const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const b = await prisma.booking.findUnique({ where: { bookingCode: 'MVN-2026-7200' }, include: { orderTransactions: true } }); if (b) { console.log('Pending Refund:', b.pendingRefund); console.log('Total Refunded:', b.totalRefunded); console.log('Current Total:', b.currentTotal); console.log('Total Paid:', b.totalPaid); } else { console.log('Booking not found'); } } check().finally(() => process.exit(0));
