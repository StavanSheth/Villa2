const fs = require('fs'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { try { const b = await prisma.booking.findUnique({ where: { bookingCode: 'MVN-2026-7200' }, include: { orderTransactions: true, bookingEvents: true } }); console.log('BOOKING_FOUND', !!b); if (b) console.log(b.bookingCode, b.status, b.pendingRefund); } catch(e) { console.error('ERROR', e); } } check().finally(() => process.exit(0));
