const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const b = await prisma.booking.findUnique({ where: { bookingCode: 'MVN-2026-7200' }, include: { orderTransactions: { orderBy: { srNo: 'asc' } }, events: true } }); if (b) { b.orderTransactions.forEach(tx => console.log(tx.actionType, 'OrderTotalDelta:', tx.orderValueDelta, 'PendingRefund:', tx.newPendingRefund, 'TotalRefunded:', tx.newTotalRefunded)); } } check().finally(() => process.exit(0));
