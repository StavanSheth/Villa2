const fs = require('fs'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const b = await prisma.booking.findUnique({ where: { bookingCode: 'MVN-2026-7200' }, include: { orderTransactions: true, bookingEvents: true } }); fs.writeFileSync('output.txt', JSON.stringify(b, null, 2)); } check().finally(() => process.exit(0));
