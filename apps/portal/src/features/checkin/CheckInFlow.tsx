import React from 'react';

export default function CheckInFlow({ bookingId }: { bookingId: string }) {
  return (
    <div className="text-white">
      Check-In Flow for Booking: {bookingId} (Feature Coming Soon)
    </div>
  );
}
